{
  "name": "barbrain-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end smoke suite for the BarBrain stack.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
